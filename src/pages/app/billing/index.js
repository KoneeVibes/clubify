import { BillingWrapper } from "./styled";
import { Layout } from "../../../containers/layout/index";

export const Billing = () => {
    return (
        <Layout>
            <BillingWrapper>

            </BillingWrapper>
        </Layout>
    )
}