import { BillingWrapper } from "./styled";
import { Layout } from "../layout/index";

export const Billing = () => {
    return (
        <Layout>
            <BillingWrapper>

            </BillingWrapper>
        </Layout>
    )
}