import { EventsWrapper } from "./styled";
import { Layout } from "../layout/index";

export const Events = () => {
    return (
        <Layout>
            <EventsWrapper>

            </EventsWrapper>
        </Layout>
    )
}